// Initialize app state management
import { appState } from './appState';

// The appState will be initialized when imported
// This file ensures it's loaded early in the application lifecycle
console.log('App state initialized');

export { appState };